# Example environment configuration for the Shelter‑Stead API

# MongoDB connection string
MONGO_URI=mongodb://localhost:27017/shelterstead

# Secret key used to sign JSON Web Tokens (replace with a strong secret)
JWT_SECRET=replace_me_with_a_secret_key

# Stripe secret key used for payments.  Obtain this from your Stripe dashboard.
STRIPE_SECRET_KEY=sk_test_replace_with_your_key

# Port for the Express server to listen on
PORT=5000
